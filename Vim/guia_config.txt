- instalar vim desde 0 (no marcar la opcion set vim plugin)

- instalar git
- agregar la ruta bin de git all PATH del sistema

- instalar curl
en Windows 
	descargar desde: https://curl.haxx.se/download.html

- crear un cmd llamado curl.cmd (posiblemente este ubicado en esta carpeta) con

#####################
@rem Do not use "echo off" to not affect any child calls.
@setlocal

@rem Get the abolute path to the parent directory, which is assumed to be the
@rem Git installation root.
@for /F "delims=" %%I in ("%~dp0..") do @set git_install_root=%%~fI
@set PATH=%git_install_root%\bin;%git_install_root%\mingw\bin;%PATH%

@if not exist "%HOME%" @set HOME=%HOMEDRIVE%%HOMEPATH%
@if not exist "%HOME%" @set HOME=%USERPROFILE%

@curl.exe %*
#####################

- copiarlo en la carpeta bin de git

- clonar vundle en la carpeta vimfiles/bundle/vundle

>> git clone https://github.com/gmarik/vundle.git vimfiles/bundle/vundle

agregar la siguiente conf al _vimrc o usar el personalizado

#############
set nocompatible              " be iMproved, required
filetype off                  " required

" set rtp+='~/vimfiles/bundle/vundle/'
" let path='~/vimfiles/bundle'

set rtp+=$VIM/vimfiles/bundle/vundle/
" let path='$VIM/vimfiles/bundle'
call vundle#rc()

Bundle 'gmarik/vundle'

filetype plugin indent on 
#############

abrir vim y ejecutar :BundleInstall con eso instalara el resto de los paquetes


OTROS

/colors 		#theme rDark
/snippets		#snippets personalizados


===================================================================

Instalacion en Linux (Debian)

-- Instalar curl
$ sudo apt-get install curl 

clonar
------
$ git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim

-- instalar ctags

$ sudo apt-get install ctags

-- situarse en ~/. (HOME)
copiar el archivo .vimrc

abrir vim y ejecutar :BundleInstall con eso instalara el resto de los paquetes

dentro de la carpeta ~/.vim crear la carpeta colors y copiar el archivo 
rDark.vim (ubicado en la carpeta colors) dentro de ella.







